//             بسم الثالوث الاقدس
#define BIGINTEGER_H
#include<iostream>	
#include<algorithm>
#include<cstdio>
#include<sstream>
#include<cmath>
#include<climits>
#include<string>
#include<iomanip>
#include<vector>
using namespace std;
//********************************************************************************************
//******************************************************************************************** 

/*
jesus help me.......
jesus help me.......
jesus help me.......
*/

int main()
{
	std::ios_base::sync_with_stdio(0);
	cin.tie(NULL); cout.tie(NULL);
	
	string s1, s2;
	while (cin >> s1)
	{
		cin >> s2;
		reverse(s1.begin(), s1.end());
		reverse(s2.begin(), s2.end());
		vector<int>v1(600,0);
		for (int i = 0; i < s1.size(); i++)
		{
			for (int j = 0; j < s2.size(); j++)
			{
				v1[i + j] += (s1[i] - '0')*(s2[j] - '0');
			}

		}
		for (int i = 0; i < 600-1; i++)
		{
			v1[i + 1] += (v1[i] / 10);
			v1[i] = v1[i] % 10;
		}
		int i = 599;
		while (i> 0 && v1[i] == 0) {
			i--;
		}
		for (; i >= 0; i--)
		{
			cout << v1[i];
		}

		cout << endl;

	}

	system("pause");
	return 0;
}
/*
jesus help me.......
jesus help me.......
jesus help me.......
*/
